<div class="heading">
  <h1>All Users</h1>
  <hr>
  <div class="well">
    Click the "Add a user" button below to create a new user. Users can log into a site, view published training sessions, leave comments, etc. Users can also have "viewers", which are a group of individuals sharing a single user account.
  </div>
  <div class="pull-right">
  <%= form_tag users_path, :method => 'get', :class => "form-search" do %>
    <div class="input-append">
      <%= text_field_tag :search, params[:search], :class => "input-medium search-query" %>
      <%= button_tag "Search", :name => nil, :class => "btn" %>
    </div>
  <% end %>
  </div>
  <p><%= link_to "Add a user", new_user_path, :class => "btn btn-large btn-success" %></p>
</div>
<div id="users">
  <%= render @users %>
  <%= will_paginate @users %>
</div>