<%= form_for SiteTrainingSession.new, :html => { :class => 'inline' } do |f| %>
  <%= f.hidden_field :training_session_id, :value => training_session_id %>
  <%= f.hidden_field :site_id, :value  => current_site.id %>
  <%= f.submit "Add To Site", :class => "btn" %>
<% end %>