<%= form_for @tag do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => "control-label" %>
    <div class="controls">
      <%= f.text_field :name %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn" %>
  </div>
<% end %>